import React from "react";
import InputToken from "..";

const InputTokenDefault = () => {
    return (
        <InputToken
            title="Digite o código"
            text="Enviamos um código de segurança para o cliente via SMS para o número: (34) 99191-9091"
        />
    )
}

export default InputTokenDefault